/* jQuery */
( function( $ ) {

    // Exécution après chargement du complet du DOM
    $(() => {
        // EXCÉCUTION DES FONCTIONS DE LA MODALE DE CONTACT
        openCloseModal()
        addRef()
    })

} )( jQuery )